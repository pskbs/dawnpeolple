# 결정 기록 (Decisions Log)

날짜순으로 주요 기술/기획 결정과 근거를 기록합니다. 요청서(`design concept/Claude outputs/02_ClaudeCode_요청서_v2.md`)와 공식 문서가 다를 때는 여기 기록하고 사용자에게 알립니다.

## 2026-09-21 — Phase 0: 앱인토스 공식 문서 조사 결과 반영

출처: `docs/apps-in-toss-notes.md` (앱인토스 개발자센터 조사)

1. **빌드 스택**: 요청서는 "정적 Vite 번들 업로드"를 가정했으나, 공식 예제는 `@apps-in-toss/web-framework` SDK + `@apps-in-toss/devtools`(`ait` CLI)를 필수로 사용함(`ait init`/`ait build`/`ait deploy`, `granite.config.ts` 설정 파일 필요). **Phase 2(앱인토스 연동)에서 반영 예정.** Phase 0/1(로컬 웹 개발)에는 영향 없음 — 플랫폼 어댑터 구조(`src/platform/`)로 격리되어 있어 웹 개발에는 지장 없음.
2. **mTLS ↔ Supabase Edge Function**: 앱인토스 공식 문서는 토스 로그인 서버 API에 mTLS 클라이언트 인증서가 필수라고 명시. Supabase Edge Function이 mTLS를 지원하지 않는다는 정보는 이번 조사로 확인/반증하지 못함(앱인토스 문서 범위 밖). **Phase 2에서 Vercel Node.js 서버리스 함수 mTLS PoC로 별도 검증 예정**(요청서 7-3 그대로 진행).
3. **탭바**: 토스 공식 플로팅 탭바만 허용, 커스텀 탭바 금지 — 요청서 가정과 일치. 그대로 진행.
4. **테마(라이트/다크)**: 검수 체크리스트에 "미니앱 테마는 라이트 모드로 구현" 항목이 명시적으로 존재. 요청서의 `hybrid`(제출용)/`night`(웹용) 2테마 전략과 방향이 일치함 — **앱인토스 제출본은 `hybrid` 고정, `night` 허용 여부는 검수팀에 별도 확인 필요**(요청서 10-3 그대로).
5. **⚠️ 벙개모임 서비스 분류 리스크 (신규, 요청서에 없던 리스크)**: 앱인토스 `intro/caution.md`는 "근처 사용자 탐색", "모르는 사람과의 연결 기능"이 있으면 "만남/소개팅 서비스"로 분류될 수 있다고 명시. "새벽사람들"의 부천 지역 기반 오프라인 벙개(낯선 사람과의 오프라인 매칭)가 이 기준에 해당될 가능성이 있음. 연애 목적이 아니라는 점만으로 자동 면제되는지는 문서로 확인 불가.
   - **영향**: 이 카테고리로 분류되면 실명 인증, 24시간 신고 대응 SLA, 반복 위반자 영구 정지 등 훨씬 무거운 요건이 추가됨.
   - **대응**: 사용자가 앱인토스 채널톡으로 서비스 컨셉("연애 목적 아님, 커뮤니티+모임")을 설명하고 카테고리 분류를 사전 확인하기로 함(12장 "제가 직접 해야 하는 일"에 추가 필요).
   - **코드 영향**: 지금 단계에서는 요청서의 신고·차단·개설 제한 설계(5-3)를 그대로 진행하되, 혹시 모를 강화 요건(실명인증 등)에 대비해 확장 가능한 구조로 설계.
6. **동의 항목(생일) 필수 여부**: 콘솔에서 "생일을 필수 동의로 설정 가능한지"는 문서로 확인 못함 — 콘솔 화면에서 직접 확인 필요(요청서 12장에 이미 사용자 할 일로 존재).
7. **TDS 패키지명**: WebView용 정확한 npm 패키지명을 문서에서 확정하지 못함(추정: `@toss/tds-mobile`). Phase 2 진입 시 재확인.

## 2026-09-21 — Phase 0: 초기 설정

- 저장소명: `dawnpeolple` (GitHub `pskbs` 계정) — 최초 논의 시 `dawnpeople`로 제안했으나 실제 생성된 저장소명(및 로컬 프로젝트 폴더명)에 맞춰 `dawnpeolple`로 통일 (2026-09-21)
  - **2026-09-23 정정**: `dawnpeolple`은 오타였고 원래 의도한 이름이 `dawnpeople`이 맞다고 확인(사용자). GitHub 저장소를 `pskbs/dawnpeople`로 리네임, `package.json` name, 로컬 스토리지 키(`dawnpeople:last-email`), CLAUDE.md 반영 완료. 로컬 프로젝트 폴더명(`2.dawnpeolple`)은 그대로 둠(리네임 요청 없었음, 경로 참조가 많아 별도 확인 후 변경).
- 패키지 매니저: pnpm (사용자 PC에 corepack 활성화가 관리자 권한 문제로 실패해 `npm install -g pnpm`으로 대체 설치)
- Vite 스캐폴드: `create-vite` 최신 버전이 기본적으로 ESLint 대신 **oxlint**를 사용함 — 요청서에 특정 린터 지정이 없어 그대로 채택. 문제 발생 시 ESLint로 교체 가능.
- `build:toss` 스크립트는 현재 `build:web`과 동일한 placeholder임. Phase 2에서 `ait build`/`ait deploy`를 반영해 교체 예정.

## 2026-09-21 — Supabase DB 접근 방식 확정

- Supabase Personal Access Token(`sbp_...`)을 받아 **Supabase Management API**(`https://api.supabase.com/v1/projects/{ref}/database/query`)로 마이그레이션을 직접 적용하는 방식으로 확정. anon/service role key는 DDL(테이블 생성 등)을 못 하지만, Management API는 Postgres에 직접 SQL을 실행할 수 있음.
- Supabase MCP 서버도 `claude mcp add`로 등록해둠(local scope, git에 안 올라감) — 다음 세션부터 도구로 사용 가능. 이번 세션은 Management API로 직접 처리.
- `0001_init.sql` 마이그레이션 적용 완료 확인: 테이블 18개, RLS 정책 43개, 함수 5개, `app_settings` 4행.
- **주의**: Personal Access Token은 Supabase 계정 전체에 대한 강력한 권한이라 각별히 조심. `.env`/커밋에 절대 포함하지 않음, 대화 기록에도 남기지 않도록 이후부터는 값 자체를 반복 출력하지 않기로 함.

## 2026-09-21 — 인증/온보딩/수다방 1차 구현, 단순화한 부분

- **이메일 자동 확인은 프로젝트 전체 설정으로 켜지 않음**(보안 완화이므로 harness가 자동 차단). 대신 자동화 테스트는 서비스 역할 키의 관리자 API(`auth.admin.createUser({ email_confirm: true })`)로 테스트 전용 유저를 만들어 우회. 실제 사용자 가입은 여전히 이메일 확인 절차를 거침.
- **profiles RLS가 본인/관리자만 조회 가능**해서(0001_init.sql) 수다방에서 다른 사람 닉네임이 안 보이는 문제 발견 → `profile_cards` 뷰(0003)로 닉네임·근무유형만 공개, 성별·출생연도는 여전히 비공개.
- **온보딩 지역 선택 단순화**: 시/도는 경기/서울/인천/기타 4개만 드롭다운, 시/군/구는 자유 텍스트 입력. 요청서 5-7의 "전국 행정구역 공공데이터 JSON"은 아직 미반영 — **TODO**.
- **로그인 방식은 이메일+비밀번호**로 구현(요청서는 매직링크 우선이었으나, 비밀번호 방식이 자동 테스트에 더 적합해 우선 채택. 매직링크 전환은 추후 검토).
- 테스트: `tests/e2e/feed-flow.spec.ts` (Playwright) — 회원가입→로그인→온보딩→수다방 글쓰기 전체 플로우 통과 확인. 스크린샷 `docs/screenshots/`.
- 다음으로 이어갈 것: 벙개모임 실동작(목록/개설/참석), 신고·차단 UI, 좋아요·댓글, AI 필터 연동, 전국 행정구역 데이터, 매직링크 전환 여부 결정.

## 2026-09-21 — 비회원 열람, 소모임 실동작, 좋아요·댓글, 디자인 적용

- **메뉴 표시명 변경**: "벙개모임" → "소모임"(사용자 요청). `TAB_LABELS.bungae`, `BUNGAE_COPY` 등 화면 문구만 변경했고, **내부 코드/라우트/테이블 명칭(`bungae`, `bungaes` 등)은 그대로 유지**해 DB 마이그레이션 범위를 최소화함. CLAUDE.md 메뉴 항목도 함께 갱신.
- **비회원 열람 모드 도입**: `App.tsx`를 전면 로그인 게이트에서 라우트별 게이트로 변경. 수다방(`/feed`)·소모임 목록/상세(`/bungae`, `/bungae/:id`)는 비로그인 상태에서도 조회 가능. 글쓰기·좋아요·댓글·소모임 개설·참석 신청은 각 액션 시점에 `/me`(로그인 진입점)로 유도. `posts`/`comments`/`bungaes`의 기존 RLS 정책이 이미 `status` 조건만으로 열려 있어(익명 조회 차단 없음) 추가 정책 변경 없이 비회원 SELECT가 동작함을 확인.
- **가입 시 정보 수집**: 기존 온보딩 RPC(`complete_onboarding`)가 닉네임(SNS 표시용)·성별·출생연도를 로그인 직후 1회 수집하는 구조를 그대로 활용(이미 로그인 방식과 무관하게 설계돼 있어 별도 변경 불필요). **토스 로그인 도입 시**: 토스는 `USER_GENDER`/`USER_BIRTHDAY` 동의 항목을 제공하지만(`docs/apps-in-toss-notes.md`) 닉네임은 제공하지 않으므로, 온보딩 화면에서 성별/생년은 프리필 후 확인만 받고 닉네임은 지금처럼 자동 생성값을 계속 쓰는 방향으로 Phase 2에서 반영 예정.
- **소모임 1차 실동작 구현**: 목록(`BungaeListPage`)/개설(`BungaeCreatePage`)/상세(`BungaeDetailPage`) 추가. 개설자는 생성 직후 `join_bungae` RPC로 자동 참석 처리(정원에 포함). 참석자 명단·대화(`bungae_comments`)는 기존 RLS 그대로 참석자+리더+관리자만 조회. **단순화**: 신고/차단 UI, 하루 2건·동시 3건 개설 한도의 클라이언트 측 안내는 아직 미구현(서버 RPC/RLS에도 아직 강제 로직 없음) — TODO.
- **좋아요/댓글 실동작**: 수다방 글에 좋아요 토글·평면 댓글(1단계 답글 없이) 추가. `posts.like_count`/`comment_count`를 애플리케이션이 아닌 DB 트리거로 유지하도록 `supabase/migrations/0004_counters.sql` 추가 및 원격 프로젝트에 적용 완료(기존 세션에서 합의된 "마이그레이션 직접 적용" 방식 유지). **단순화**: 답글(대댓글)은 이번 범위에서 제외 — 추후 `parent_id` 활용해 1단계 스레드 UI 보강 필요.
- **디자인 적용**: `docs/design-notes.md`의 클레이(clay)+알약(pill) 시스템을 `src/styles/components.css` 공용 클래스(`clay-card`, `pill-button`, `pill-input`, `squircle-badge`, `night-sky`)로 구현해 로그인/온보딩/내정보/수다방/소모임 화면에 적용. 달·별 아이콘은 `public/icons.svg`에 추가(파티클 애니메이션 없음, 규칙 준수). 마스코트("새벽이들") 일러스트는 이번 범위에 없음 — 실제 캐릭터 아트는 별도 디자인 작업 필요.
- 다음으로 이어갈 것: 신고·차단 UI, 소모임 개설 한도(하루 2건·동시 3건) 서버측 강제, 답글(대댓글) UI, AI 필터 연동, 전국 행정구역 데이터, 매직링크 전환 여부, 마스코트 일러스트 제작, 토스 로그인 PoC.

## 2026-09-21 — 디자인 시스템 v2(글래스·블롭) + 수다방 SNS 피드 리디자인

- **요청 배경**: 사용자가 기존 UI를 "너무 AI틱하고 촌스럽다"고 판단, `design concept/design2.png`(연보라 글래스모피즘 AI 챗봇 앱 UI킷)를 새로 추가하며 질감·색상 전부 따라해달라고 요청. 수다방은 인스타그램/스레드 같은 SNS 피드 UX로 바꿔달라는 요청도 포함.
- **⚠️ 요청서(색상 그대로)와 공식 문서 충돌**: CLAUDE.md 절대규칙 2번은 라이트모드(OS 다크모드 포함) 금지, night 테마는 전면 딥네이비를 명시하고, `docs/design-notes.md`에는 이미 "파스텔 보라→네이비+라벤더+달빛노랑" 치환 원칙이 기록돼 있음. 사용자에게 확인한 결과 **팔레트는 네이비 유지, 디자인컨셉에서는 질감·구성(글래스 카드, 캡슐 탭바, 그라데이션 블롭, 아이콘 액션행)만 반영**하기로 함(`docs/design-notes.md`의 design2.png 분석 섹션 참고).
- **적용 내용**: `components.css`에 `.glass-panel`(프로스티드 글래스)·`.blob-art`(정적 그라데이션 블롭, 파티클 애니메이션 없음)·`.avatar`(닉네임 첫 글자 원형 배지)·`.icon-action`(아이콘 전용 좋아요/댓글) 신설. 탭바를 아이콘 전용 캡슐+활성 원형 배지로 축소. 수다방 피드를 박스 카드 목록에서 구분선 기반 스레드 리스트로 재구성하고, 정렬 토글("지금"/"인기")과 Threads식 한 줄 컴포저(포커스 시 확장)를 추가.
- **단순화**: 상대 시간 표시(`n분 전`)는 외부 날짜 라이브러리 없이 `src/lib/format.ts`에서 직접 계산. "인기" 정렬은 서버 재조회 없이 이미 불러온 50개를 클라이언트에서 재정렬(추후 게시물이 많아지면 서버 정렬 쿼리로 전환 필요 — TODO).
- 다음으로 이어갈 것: 마스코트("새벽이들") 일러스트 실제 제작(현재는 달·별 아이콘 장식만 있음), 소모임 카드에도 아바타/블롭 장식 확장 여부 검토.

## 2026-09-21 — 디자인 v3: 디자인 컨셉 색상까지 그대로 + 수다방 SNS 전면 개편

- **요청**: 사용자가 v2(네이비 유지 + 질감만)도 "AI틱하고 촌스럽다"며 **색상까지 design concept 그대로** 따라달라고 명시적으로 재요청. 모바일 우선, 인스타그램·스레드 느낌.
- **결정(이전 결정 번복)**: `hybrid` 테마를 컨셉의 라벤더 글래스 팔레트(배경 #f3f0fb, 보라 #8465f2→#b29cff 그라데이션, 흰 반투명 글래스 카드)로 재정의하고 **앱인토스·웹 공통 기본값**으로 변경(`main.tsx` 기본값, `.env.example`). `night`는 같은 토큰 이름의 딥네이비 선택형 변형으로 유지. 라이트 톤이라 앱인토스 "라이트 모드만" 규칙과도 맞음. CLAUDE.md 절대규칙 2번·디자인 항목 갱신.
- **폰트**: Pretendard(jsDelivr CDN, 무료 OFL). 앱인토스 빌드에서 외부 CDN 허용 여부는 Phase 2에서 확인 — 안 되면 로컬 번들로 전환(TODO).
- **수다방 구조 변경**: 목록 안 펼침 댓글 → **스레드 상세 화면(`/feed/:id`)** + 하단 고정 답글 입력 바. 글쓰기는 **전체 화면 작성(`/feed/new`)**. 피드 상단에 인사 + 컨셉 헤드라인 + "Ask me anything"식 컴포저 바, **다가오는 소모임 스토리 행**(실데이터, 탭하면 소모임 상세). 본문 두 번 탭 = 좋아요(하트 팝 애니메이션 — 별·파티클 아님).
- **탭바**: 글래스 캡슐(활성 탭만 라벨 노출) + 보라 원형 `+` 버튼(수다방에선 새 글, 소모임에선 소모임 만들기). 글쓰기·상세 화면에서는 탭바 숨김. 앱인토스 빌드에서는 TDS 탭바로 교체해야 하는 점은 그대로.
- **소모임**: 날짜 타일·시간/장소 메타·남은 자리 진행바 카드, 상세는 참석 현황 좌석 점 + 참석자 대화를 채팅 말풍선(내 글 오른쪽 보라)으로. 미참석자는 여전히 집계만 노출(규칙 7 유지).
- **내정보**: 인스타 프로필식(그라데이션 링 아바타, 게시글/받은 좋아요/참여 소모임 통계, 내가 쓴 글).
- **테스트**: Playwright를 모바일 뷰포트(Pixel 7)로 전환, 새 흐름에 맞게 수정 — 통과. 스크린샷 `docs/screenshots/`.
- **발견한 데이터 이슈(미처리)**: 과거 E2E 실행이 남긴 테스트 글들이 작성자 삭제 후 "알 수 없음"으로 피드에 남아 있음. 운영 DB 데이터 삭제라 사용자 확인 후 정리 필요. 앞으로는 E2E 테스트가 만든 글도 afterAll에서 지우도록 보강 필요(TODO).
- **다음 후보**: 신고·차단 UI(글 ··· 메뉴), 답글의 1단계 답글, 무한 스크롤, 마스코트 일러스트.
## 2026-09-22 — 개발 중 Supabase 메일 인증(Confirm email) 끄기

- **배경**: 가입 테스트 중 "email rate limit exceeded" 발생. Supabase 기본 SMTP는 시간당 약 2통만 발송해 반복 가입 테스트가 막힘.
- **결정**: 개발 기간 동안 Supabase 대시보드 → Authentication → Sign In / Providers → Email → **Confirm email 끄기**(사용자가 직접 설정). 로그인·가입 에러는 영어 원문 대신 해요체 안내로 표시하도록 수정(`AUTH_ERROR_COPY`).
- **⚠️ 출시 전 필수 TODO**: Confirm email 다시 켜기 + 커스텀 SMTP(Resend 등) 연결. 새 외부 계정·API 키가 필요하므로 진행 전에 사용자 확인.

## 2026-09-22 — 테마를 hybrid 하나로 확정

- **결정**: 사용자 선택형 테마 없이 `hybrid`(라벤더 글래스)를 앱인토스·웹 모든 사용자에게 동일 적용.
- **이유**: `night`(전면 딥네이비)는 앱인토스 "라이트 모드만" 검수에서 다크모드로 보일 위험, 화면마다 두 테마 QA 부담, 초기 브랜드 인상 통일.
- **처리**: `night.css`와 `VITE_THEME` 스위치는 삭제하지 않고 보관(토큰 기반이라 유지 비용 거의 없음). 배포 빌드에는 쓰지 않음. 추후 웹·스토어에서 야간 모드 수요가 생기면 사용자 선택 기능으로 재검토.

## 2026-09-22 — SNS 기능 대폭 확장(사용자 피드백 반영)

- **수다방 컨셉 정정**: "퇴근길"이 아니라 "새벽에 일하는 사람들이 쓰는 SNS". 상단 인사·컨셉 문구·컴포저 바·내정보 버튼·다가오는 소모임·지금/인기 정렬을 제거하고, 상단은 "새벽사람들 · 새벽에 일하는 우리끼리의 공간" + DM 버튼. 글쓰기는 탭바 + 버튼으로만.
- **글**: 글자수 제한 없음(DB 기술 상한 20,000자), 다른 사람이 볼 때 500자 넘으면 "더 보기". 사진·동영상·파일 첨부(최대 10개, 이미지 10MB·영상 50MB·파일 20MB). 여러 장이면 스레드처럼 가로 스와이프 + 탭하면 전체 화면 보기. 내 글 삭제.
- **답글**: 대댓글(항상 최상위 댓글 아래에 모으고, 답글의 답글은 @닉네임), 사진 첨부(최대 4장), 본인 답글 수정·삭제. 소모임 참석자 대화도 같은 컴포넌트(`CommentSection`) 사용 — 채팅 말풍선 UI는 DM으로 옮김.
- **소모임**: "다가오는 소모임" 줄을 소모임 탭 상단으로 이동. 기본 모이는 시각 새벽 6시(오전 6시). 신고 메뉴 추가.
- **⚠️ 규칙 변경 1 — 참석 전 성별·연령대 공개**: 기존 절대규칙 7("미참석자에게는 남은 자리만")과 충돌. 사용자 요청으로 **집계만**(여성 2·남성 1 / 20대 2·30대 1, `get_bungae_demographics`) 공개하고, 닉네임·세부 연령대(예: 20대 후반)는 참석자·리더에게만(`get_bungae_participants`). `FEATURES.preJoinDemographics`로 끌 수 있음. 참석자가 1~2명이면 사실상 특정될 수 있다는 점은 법률 검토 포인트에 기록.
- **⚠️ 규칙 변경 2 — DM 도입**: 요청서 원안은 "1:1 DM 없음"(만남 서비스 분류 회피 목적). 사용자 요청으로 추가하되 `FEATURES.dm`으로 끌 수 있게 함. 안전장치: 차단 시 메시지 불가(RLS), 신고 메뉴, 대화방 상단 안전 안내, 첨부는 비공개 버킷(`dm-media`, 대화 참여자만 서명 URL로 열람). 앱인토스 검수 전에 유지 여부 재확인 필요.
- **프로필(내정보)**: 스레드식 — 닉네임·근무 유형 배지·사진·한줄소개·팔로워/팔로잉, 탭(수다글/답글/소모임). 다른 사람 프로필(`/u/:id`)에서 팔로우·메시지·신고·차단. 팔로워/팔로잉 목록(검색). 프로필 편집(사진·닉네임 7일 1회·소개·근무 유형·퇴근 시간대·지역). 설정(톱니바퀴): 차단 목록, 내 기본 정보(나에게만), 약관, 작은 텍스트 버튼으로 로그아웃·회원탈퇴.
- **가입**: 프로필 사진(선택, 없으면 기본 이미지)·한줄소개(선택) 추가. 약관 동의에 "모두 동의" + 각 항목 ">" 버튼으로 약관 전문 팝업.
- **약관 초안 작성**: 이전에 약관 본문이 없었음을 확인하고 `src/config/legal.ts`에 이용약관·개인정보 처리방침 초안(draft-2)을 작성. `/terms`, `/privacy` 공개 페이지(앱인토스 콘솔 등록용). 검토 포인트는 `docs/terms-review-points.md`.
- **회원탈퇴 구현**: 스토리지 파일(내 폴더·내 대화방 폴더) 삭제 → `delete_my_account` RPC(글·답글·소모임·대화·계정 삭제). 요청서 6-3과 일치.
- **보안 보강(0006)**: 기존 `profiles_update_own` 정책이 role·status까지 수정 가능했던 문제를 가드 트리거로 차단(허용 컬럼만). posts의 like_count 등 조작도 차단. 트리거 함수의 API 직접 호출 차단, 로그인 전용 RPC의 anon 실행 권한 회수.
- **마이그레이션**: 0005~0008을 원격 프로젝트에 직접 적용(기존 합의 방식). 0007·0008은 대댓글 RLS 정책 버그(컬럼 참조, 정책 재귀) 수정.
- **테스트**: E2E를 새 흐름(온보딩 약관 팝업·사진, 첨부 글, 대댓글·수정·삭제, 프로필 편집, 팔로우·DM·차단, 새벽 6시 기본값, 참석 현황 집계, 회원탈퇴)으로 전면 수정 — 통과. 테스트 계정·데이터·파일은 종료 시 삭제.
- **남은 일**: 알림(좋아요·답글·팔로우·DM 푸시), 글 수정, 사진 EXIF 제거, 소모임 개설 한도 서버 강제, AI 필터 연동, 앱인토스 빌드에서 TDS 탭바 교체.

## 2026-09-22 — 사용자 피드백 반영(로그인·소모임·차단·어드민·약관·저장공간)

- **로그인 화면**: 로그인 카드 아래 "처음 오셨나요? / 가입하기" 카드로 가입 유도 강조. 마지막 로그인 이메일을 기기에 기억해 자동 입력(비밀번호는 저장 안 함).
- **자동 로그인**: Supabase 세션을 localStorage에 저장 + 자동 갱신(`persistSession`, `autoRefreshToken` 명시). 로그아웃 전까지 유지. Supabase 대시보드 Auth → Sessions의 "Time-box user sessions"·"Inactivity timeout"은 **꺼진 상태(기본값)로 유지**해야 함.
- **앱인토스 로그인**: 공식 문서상 미니앱은 **토스 로그인만 허용** → 앱인토스 빌드에서는 이메일 로그인 화면을 보여주지 않음(Phase 2). 웹은 이메일 로그인 유지. 같은 사람이 웹(이메일)과 토스를 둘 다 쓰는 경우 계정 연결은 Phase 2에서 설계.
- **아이디·비밀번호 찾기**: 아이디 = 가입 이메일. 이메일 입력 → `api/auth/reset-password.ts`(Vercel 서버 함수)가 임시 비밀번호를 만들어 Gmail SMTP(`wol100st2@gmail.com`)로 발송하고, **메일 발송이 성공한 뒤에만** 비밀번호를 바꿈. 가입 여부 노출 방지(항상 같은 응답), 같은 이메일 10분 1회·전체 시간당 200회 제한(`password_reset_requests`, 이메일 해시만 저장). 내정보 → 설정 → **비밀번호 변경**(지금 비밀번호 확인 후 변경) 추가.
  - **보안 참고**: "새 비밀번호를 메일로 발송"은 사용자 요청 방식. 표준 방식(재설정 링크)보다 약함 — 남의 이메일을 입력하면 그 사람 비밀번호가 바뀔 수 있음(메일은 본인에게만 가므로 탈취는 아님). 문제가 되면 Supabase `resetPasswordForEmail` 링크 방식으로 전환.
  - 로컬 개발에서도 `pnpm dev`에서 `/api/*`가 동작하도록 `vite.config.ts`에 로컬 API 미들웨어 추가.
  - 임시 비밀번호 형식은 **소문자+숫자 조합 6자리**로 쉽게 생성(2026-09-22 변경, 사용자 요청 — 기존 대소문자+숫자 10자리는 손으로 옮겨 적기 불편함). 헷갈리는 글자(0/o, 1/l)는 제외.
- **소모임**: 상단 메시지(DM) 아이콘 제거, "부천" 배지를 오른쪽 위로 옮겨 그라데이션으로 강조. 참석 현황은 "남성(30대), 여성(20대)"처럼 한 사람씩 나열(닉네임 없이, 기존 집계 RPC 그대로 사용 → 개인 식별 정보 추가 노출 없음).
- **신고**: 사유 목록에 "직접 입력" 추가(`reason='직접 입력'`, 내용은 `reports.detail`, 500자 제한).
- **어드민**: `/admin` 신고 관리 화면(profiles.role='admin'만 접근, RLS 기존 정책 사용). 상태(대기/확인 중/조치 완료/기각) 변경, 대상 보러 가기, 숨기기/다시 보이기(글·답글·소모임 대화는 hidden, 소모임은 cancelled). Supabase 대시보드에서는 `reports_overview` 뷰로 신고자 닉네임까지 한눈에 확인. 사용자 정지 기능은 아직 없음.
- **차단 안내**: 나를 차단한 사람에게 팔로우·메시지 시도 시 "상대방이 차단했어요". `blocked_me(p_other)` RPC(true/false만 반환), `start_conversation` 에러 문구 분리.
- **뒤로가기**: `AppBar`의 뒤로 버튼이 `navigate('/dm')`처럼 "앞으로" 이동해 기록이 꼬이던 문제 수정 → `useGoBack`(앱 안 기록이 있으면 history back, 없을 때만 fallback). 글쓰기 취소·작성 완료, 프로필 편집 저장, 글 삭제 후에도 원래 화면으로 돌아감.
- **수다방**: 글쓰기 플레이스홀더 "새벽에 우리끼리 공유해요".
- **저장공간**: **동영상 업로드 중단**(`FEATURES.videoUpload=false`) — 동영상은 저장·전송(egress) 비용이 사진의 수십 배라 무료/Pro 한도를 가장 먼저 소진. 올리려 하면 "지금은 용량 제한으로 동영상은 올릴 수 없어요. 사진만 올려주세요." 대신 1MB 넘는 사진은 올리기 전 긴 변 1920px JPEG로 자동 압축(부수 효과로 위치정보 등 EXIF도 제거). `media` 버킷 파일당 한도 50MB → 20MB.
- **약관**: 모든 이메일 `wol100st2@gmail.com`, 사업자등록번호·주소 삭제, 시행일 2026년 9월 22일, 버전 draft-3.
- **푸시 알림**: 보류. 앱인토스 미니앱(WebView)에서 파트너가 직접 푸시를 보내는 방법은 이번 조사 범위에서 확인하지 못함 → Phase 2에서 공식 문서로 확인. 그 전까지는 앱 안 안 읽은 메시지 배지로 대체.
- **광고**: CLAUDE.md 기존 결정(글·소모임 **상세 하단 배너**, 작성·개설 완료 시 전면형) 유지. 사용자 의견("게시글 쓸 때 하단")은 광고 붙이는 단계에서 앱인토스 광고 정책과 함께 위치 확정 — 글쓰기 입력 화면 자체의 광고는 입력 방해로 검수 위험이 있어 상세·완료 화면을 우선 제안.
- **이메일 주소 표기**: 사용자가 준 `wol100st2@gmil.com`은 `gmail.com`의 오타로 보고 `wol100st2@gmail.com`으로 반영(사용자 확인 필요).

## 2026-09-22 — 지역을 전국으로 확장(부천 고정 바인딩 해제)

- **요청 배경**: 사용자가 "일단 넓혀서 한번보자"며 지역 제한을 풀어달라고 요청. 구체 범위는 후속 확인으로 확정: **전국 오픈**, 가입·소모임 개설 모두 **시/도 + 시/군/구 선택**(소모임은 **동/읍/면**까지, 구체 장소는 자유 입력 — 예: "부천역"), 회원이 **동네를 최대 3개까지 이름 붙여 저장**(집/회사 등, 당근마켓 "동네 설정" 방식)하고 그중 하나로 전환, 소모임 목록은 **현재 동네 기준 가까운 순 정렬**(지역으로 걸러서 숨기지는 않음 — 목록이 비어 보이는 것을 원치 않음), **마감된 모임 제외** 토글 추가.
- **⚠️ 공식 문서(CLAUDE.md) 충돌**: 기존 "벙개모임: 개설 지역은 부천 고정 바인딩", "오프라인 시작 지역: 부천. `OPEN_REGIONS` 설정값으로 관리"와 정면으로 상충. 사용자에게 확인 후 진행, CLAUDE.md 해당 항목 갱신 완료.
- **데이터**: `src/config/regions.ts`에 전국 17개 시/도 + 시/군/구(다구 도시는 시 단위까지만, 구 단위 생략) 정적 목록 추가. **읍/면/동은 전국 데이터를 직접 입력하지 않고 자유 텍스트 입력으로 처리**(전국 읍면동은 수천 개라 공공데이터 연동 없이 손으로 정확하게 유지하기 어려움 — 요청서 5-7의 "전국 행정구역 공공데이터 JSON"은 여전히 TODO, 필요해지면 공공데이터 API로 대체).
- **DB**: `0010_nationwide_regions.sql` — `bungaes`에 `sido`/`sigungu`/`eupmyeondong` 컬럼 추가(기존 `region_code`는 더 안 씀, 컬럼은 남겨둠), `profiles`에 `saved_locations`(jsonb, 최대 3개 체크 제약) 추가. `profiles_guard_update` 트리거 허용 컬럼에 `saved_locations` 포함.
- **UI**: 소모임 목록 상단 지역 배지를 누르면 `LocationSheet`(동네 전환·추가·삭제)가 열림. 소모임 개설 화면에 시/도·시/군/구 선택 + 동/읍/면 입력 추가(부천 장소 칩은 시/군/구가 '부천시'일 때만 노출). 가입(온보딩)·프로필 편집의 시/군/구도 자유 텍스트에서 시/도에 종속된 드롭다운으로 변경.
- **정렬**: 실거리 계산(위경도) 없이 행정구역 계층으로 근사(`regionRank` — 같은 시/군/구+동 일치 > 같은 시/군/구 > 같은 시/도 > 그 외). 위경도 기반 정확한 거리 정렬이 필요해지면 별도 지오코딩 연동 필요(TODO).
- **삭제한 문구**: "수다방은 어디서나 이용할 수 있어요"(`FEED_NATIONWIDE_NOTICE`) — 소모임도 전국이 되면서 수다방만 따로 안내할 이유가 없어져 삭제. 소모임 목록 상단 안내문(`REGION_NOTICE`)은 "지금은 부천 지역 위주로 진행돼요" → "전국 어디서나 소모임을 열 수 있어요. 내 동네와 가까운 소모임부터 보여드려요."로 교체.
- **남은 일**: 앱인토스 콘솔 등록용 설명 문구(`docs/copy-list.md`)의 "부천 지역에서 열리는" 표현 갱신, 읍/면/동 공공데이터 연동 여부 결정, E2E 테스트를 새 지역 선택 플로우에 맞게 갱신.

## 2026-09-22 — 마케팅 정보 수신 동의 문서 신규 작성(토스 로그인 연동용)

- **배경**: 앱인토스에서 토스 로그인을 연동하려면 이용약관·개인정보 처리방침과 별개로 "마케팅 정보 수신 동의" 문서(선택 동의)가 필요함. 기존에는 없던 문서라 `src/config/legal.ts`에 `MARKETING`(버전 `draft-1`)으로 신규 작성.
- **선택 동의 원칙**: 동의하지 않아도 가입·서비스 이용 제한 없음. 이용약관·개인정보 처리방침은 필수, 이 동의만 선택.
- **라우트 추가**: `/marketing`(다른 법률 문서와 동일하게 `LegalPage` 재사용, 로그인 없이 열람 가능).
- **Notion 연동**: 사용자가 각 문서를 Notion 페이지로 옮겨 앱인토스 콘솔에 링크로 등록하기로 함. `src/config/legal.ts`의 `TERMS`/`PRIVACY`/`MARKETING`을 마크다운으로 변환해 `docs/legal-notion/`에 저장(변환 스크립트는 1회성이라 실행 후 삭제 — 문구가 바뀌면 같은 방식으로 다시 생성해야 함, `docs/terms-review-points.md` 참고).
- **미구현 상태**: 실제 동의 체크박스·철회 토글·마케팅 메일 발송 기능은 아직 없음. 문서만 먼저 준비한 상태(TODO, `docs/terms-review-points.md` 참고).

## 2026-09-22 — 약관을 "최종안"으로 정리(초안 배너 삭제, 대표→책임자)

- **요청 배경**: 사용자가 앱인토스에 최종본으로 제출해야 해서 화면에 "[초안 — 출시 전 법률 검토 필요]"·"버전 draft-3" 같은 메모성 표기가 보이면 안 된다고 요청. 회사 정보 표기도 "대표 김봉수"가 아니라 "**책임자** 김봉수"로 바꿔달라고 함(사업자 대표자는 배우자 명의 — 실제와 다른 표기라 정정).
- **처리**: `LegalDoc` 타입에서 `draftNotice`/`version`/`effectiveDate` 필드를 제거하고 `/terms`·`/privacy`·`/marketing` 화면·`docs/legal-notion/*.md`에서 해당 배너·버전 줄을 뺌(시행일 문구는 각 문서 "부칙" 조항에 이미 있어서 정보 손실 없음). `COMPANY.owner` → `COMPANY.manager`로 이름 변경, 본문의 "대표" → "책임자"로 교체. 앞서 게시해둔 Claude Docs 3개도 같은 링크를 유지한 채 내용만 수정.
- **주의(그대로 유지한 것)**: 문서 안에 남아 있는 `[확인 필요: ...]` 표시(개인정보 처리방침의 국외 이전 국가, 로그 보관 기간 등)는 지우지 않음 — 이건 "메모"가 아니라 아직 실제로 확인되지 않은 항목이라, 지우면 이미 확인된 것처럼 보여 법적으로 더 위험함. 출시 전 실제 값으로 채워야 함(`docs/terms-review-points.md` 참고).
- **내부 버전 추적은 유지**: `TERMS_VERSION`/`PRIVACY_VERSION`/`MARKETING_VERSION` 상수는 코드에 남겨둠(가입 동의 시 `terms_agreements`에 저장되는 값이라 화면 표시 여부와 무관하게 필요).

## 2026-09-23 — 광고 연동 + 토스 로그인 서버 연동 착수 + 푸시 알림 보류 해제

- **광고(구현 완료)**: `@apps-in-toss/web-framework` 설치. 글쓰기·벙개 개설 완료 시 전면 광고(`src/platform/toss/ads.ts` → `src/platform/index.ts`의 `showCompletionAd()`). 앱인토스 밖(웹)에서는 `isSupported()`로 자동 no-op. `VITE_AD_INTERSTITIAL_ID` 미설정 시 정책 위반 방지를 위해 테스트 ID(`ait-ad-test-interstitial-id`)로 동작. 같은 광고 5분 내 재노출 금지(빈도 제한, 임의로 정한 기본값 — 필요시 조정). 상세 화면 하단 배너는 아직 미구현.
- **푸시 알림(보류 해제, 2026-09-22 결정 갱신)**: 공식 문서 확인 결과 서버 트리거 가능 — `POST https://apps-in-toss-api.toss.im/api-partner/v1/apps-in-toss/messenger/send-message`(`x-toss-user-key`/`x-anon-key`, **mTLS 필요**). 앱당 분당 15,000건·사용자당 분당 10건 제한. **콘솔에서 메시지 템플릿 등록 + 검수 승인 필요**(사용자가 콘솔에서 직접 해야 하는 일). mTLS가 로그인과 공용 인프라라 로그인 PoC 완료 후 이어서 구현 예정. 아직 코드 미작성.
- **토스 로그인(서버 연동 착수, 미검증)**: `api/_lib/toss.ts`(mTLS `https.Agent` + generate-token/login-me 호출 + AES-256-GCM 복호화), `api/auth/toss/login.ts`(userKey 기반 결정론적 내부용 이메일로 Supabase Auth 사용자 생성/조회 → 매직링크 토큰 발급 → 클라이언트가 `verifyOtp`로 세션 완료). `src/platform/toss/login.ts` + `AuthPage.tsx`에서 `VITE_PLATFORM=toss`일 때 이메일 폼 대신 토스 로그인 버튼만 노출. 온보딩(`complete_onboarding`, 0012 마이그레이션, 원격 적용 완료)이 `auth.users` 메타데이터의 `toss_user_key`를 `profiles.toss_user_key`로 옮김.
  - **실제 이메일 저장 안 함**: Supabase Auth의 `email` 필드에는 `toss-<userKey 해시>@toss.dawnpeople.internal` 같은 내부용 가짜 이메일만 씀(발송 안 함, 사람이 못 씀) — 규칙 4(이메일 원문 저장 금지) 준수.
  - **[2026-09-23 정정] `TOSS_APP_KEY`/`TOSS_CLIENT_ID`/`TOSS_CLIENT_SECRET`는 존재하지 않는 개념이었음 — 제가 일반적인 OAuth2 패턴("서버 간 인증엔 보통 client_id/secret이 있다")을 근거 없이 가정해서 `.env.example`에 미리 넣어둔 것.** 실제로는 공식 문서(`documentation/common/authentication/toss-login.md`) 어디에도 이 세 용어가 등장하지 않고, mTLS 인증서만으로 서버 간 인증이 끝남 — 제가 만든 mTLS PoC도 이 세 값 없이 성공했었음(이미 검증됐던 걸 뒤늦게 재확인). 사용자가 콘솔에서 한참 찾다가 없는 걸 확인 → `.env`/`.env.example`에서 세 항목 삭제.
  - AES-256-GCM 복호화 AAD 값은 콘솔 "이메일로 복호화 키 받기" 화면에 복호화 키와 나란히 표시됨 — **고정 문자열 `"TOSS"`**. `.env`의 `TOSS_DECRYPT_AAD`에 반영 완료. 복호화 키(base64 디코드 시 32바이트, AES-256 키 길이와 일치 확인)도 `TOSS_DECRYPTION_KEY`에 반영 완료.
  - **결론: 크리덴셜 준비는 다 끝남**(mTLS 인증서 + 복호화 키 + AAD). 남은 건 진짜 인가 코드로 실제 로그인 테스트뿐 — 이건 미니앱을 앱인토스 샌드박스/실제 앱에서 `TossAuth.login()`으로 직접 실행해봐야 나오는 값이라 콘솔에서 더 찾을 게 없음(다음 단계: `ait build`/`ait deploy`로 미니앱을 만들어 샌드박스에서 실제 로그인 테스트).
  - **동의 스코프 재검토 필요**: 현재 콘솔 스코프가 "이름"으로 설정돼 있는데, `userKey`는 스코프 없이도 내려오고 구현에서 이름을 저장하지 않으므로(규칙 4) **"이름" 스코프를 콘솔에서 빼는 걸 권장** — 동의 화면이 가벼워지고 개인정보 요청도 줄어듦.
  - `TossAuth.login()`(v3 SDK, `appLogin()`은 deprecated)을 사용.
- **mTLS PoC 성공(핵심 아키텍처 리스크 해소)**: 사용자가 콘솔에서 발급받은 mTLS 인증서/키를 `.env`에 넣은 뒤, `node:https`의 `Agent({cert, key})`로 실제 토스 API(`generate-token`)에 가짜 인가 코드로 요청 → **TLS 핸드셰이크 성공 + 정상 JSON 에러 응답**(`invalid_grant`, 코드가 가짜라서 나는 정상적인 응답) 확인. 이전까지 "Supabase Edge Function이 mTLS를 지원하지 않아 Vercel Node에서 될지 안 될지 PoC 필요"였던 부분(`docs/apps-in-toss-notes.md` 불명확한 점 2번)이 **해소됨** — 별도 mTLS 지원 서버(비용 발생)는 필요 없고, 기존 `api/`(Vercel Node 런타임) 그대로 쓰면 됨.
- **로컬에서 검증 가능한 것들은 전부 테스트 완료**:
  - AES-256-GCM 복호화 왕복 테스트(실제 키+AAD로 직접 암호화→복호화) PASS.
  - Supabase 관리자 브리지(테스트 계정 생성 → `toss_user_key` 메타데이터 저장 확인 → 매직링크 토큰 발급 → 계정 삭제) 실제 프로젝트 DB에 대고 PASS.
  - `pnpm dev`의 로컬 API 미들웨어로 `/api/auth/toss/login` 핸들러 전체를 실제로 호출 — 가짜 인가 코드로 502(정상), 빈 요청으로 400(정상) 확인. 이걸로 `api/_lib/toss.ts`+`api/auth/toss/login.ts`의 전체 배선이 검증됨.
  - **여기서부터는 제가 검증 못 함**: 진짜 `TossAuth.login()` 호출은 토스 앱/샌드박스 안에서만 나옴(디바이스 필요).
- **`ait` 빌드 파이프라인 세팅 + 로컬 빌드 성공**: `@apps-in-toss/devtools` 설치, `ait init --skip-input`으로 `apps-in-toss.config.ts` 생성(appName: `dawnpeople`, brand.primaryColor: 브랜드 라벤더 `#8465f2`), `vite.config.ts`에 devtools unplugin 연결, `build:toss` 스크립트에 `&& ait build` 추가. `pnpm run build:toss` 로컬 실행 → `dawnpeople.ait`(220KB, 100MB 제한 대비 여유 충분) 정상 생성 확인.
  - **다음은 사용자만 할 수 있는 일**: `ait deploy`로 콘솔에 테스트 빌드를 올리려면 `ait token add`로 API 키가 필요한데, 이건 토스 비즈니스 계정으로 콘솔에서 발급받아야 함(정확한 발급 화면 위치 미확인 — "앱 정보" 근처로 추정). 발급받으면 `ait token add --api-key <값>`으로 등록 후 `pnpm run deploy` 실행 가능.

## 2026-09-23 — 토스 로그인 실기기 테스트 성공 (버그 3개 수정)

- **결과: 실제 토스 앱에서 로그인 성공 확인.** Phase 2 핵심 리스크(토스 로그인 서버 연동)가 완전히 해소됨.
- **버그 1 — mTLS 인증서/키가 Vercel에 잘못 저장됨**: `TOSS_MTLS_CERT_BASE64`/`TOSS_MTLS_KEY_BASE64`에 실제 base64 값이 아니라 클립보드 복사용 PowerShell 명령어 텍스트(`[Convert]::ToBase64String(...) | ... clipboard`)가 그대로 들어가 있었음(명령 실행 결과 대신 명령어 자체를 붙여넣은 실수로 추정). `error:0480006C:PEM routines::no start line`로 발현. 로컬 `.env`의 검증된 값으로 재등록해 해결.
- **버그 2 — 재로그인 시 500 에러(정규식 오타)**: `api/auth/toss/login.ts`에서 "이미 가입된 사용자면 통과"시키는 조건이 `/already.*registed/`(오타, `registered`가 아님)라 실제 Supabase 메시지("already been registered")를 못 잡고 재로그인마다 500을 반환. `createError.status === 422 || createError.code === 'email_exists'`로 교체.
- **버그 3 — verifyOtp 파라미터 오류(진짜 근본 원인)**: `src/platform/toss/login.ts`가 `supabase.auth.verifyOtp({ email, token_hash, type })`처럼 `email`과 `token_hash`를 같이 보냈는데, GoTrue는 `token_hash` 검증 시 `email`이 같이 오면 `validation_failed`("Only the token_hash and type should be provided")로 거부함. TypeScript는 `VerifyOtpParams`가 유니언 타입이라 이 조합을 통과시켜서(각 필드가 유니언의 "어떤" 멤버에는 존재) 컴파일 타임에 못 잡았음 — 백엔드가 200을 반환해도 클라이언트 마지막 단계에서 항상 실패하던 원인. `email` 제거로 해결(로컬에서 `admin.generateLink` → `anon.verifyOtp` 왕복 테스트로 검증).
- **교훈**: mTLS 인증서/키 같은 긴 값을 Vercel에 등록할 때 클립보드 경유 대신 파일 stdin으로 직접 파이프하는 걸 기본으로. `vercel env pull`은 Secret 타입 값은 못 읽어오니(당연함, "Secret"의 정의상) 재검증하려면 새 값을 등록 후 실제 API 응답/로그로 확인해야 함.
- **다음 단계(Phase 2 마무리 후보)**:
  1. 동의 스코프에서 "이름" 빼기(콘솔, 사용자가 직접) — 위 2026-09-23 항목에 이미 있던 권장사항, 아직 미처리.
  2. 상세 화면 하단 배너 광고 구현(전면 광고는 완료).
  3. 푸시 알림(메신저 API) 구현 — mTLS 인프라는 이미 있음, 코드 미작성.
  4. 실제 성별/생일 등 AES-256-GCM 복호화 필드가 진짜 데이터로 정상 디코딩되는지 확인(주석에 "미검증"으로 남아있던 부분 — 이번 로그인 성공 시 `gender` 필드가 실제로 잘 들어왔는지 Supabase `profiles` 테이블에서 확인 권장).
  5. Phase 3(검수 대비)로 넘어갈지, Phase 2 잔여 항목을 더 채울지 결정.

## 2026-09-23 — 실제 토스 로그인 테스트 실패 원인 2건 수정 + main 병합

- **배경**: 사용자가 앱인토스에서 실제로 "토스로 로그인"을 눌러봤더니 "토스 로그인에 실패했어요" 발생. 원인 조사 결과 두 가지가 겹쳐 있었음.
- **원인 1(더 결정적) — `api/auth/toss/login.ts`가 `main`에 없었음**: 그동안 로그인·SNS 확장 작업이 전부 `feat/sns-social` 브랜치에서만 진행되고 `main`은 9/21 초기 상태에 멈춰 있었음. Vercel 프로덕션(`dawnpeople.vercel.app`)은 `main`을 배포하므로 `/api/auth/toss/login`이 아예 **404**였음(`curl`로 직접 확인). **처리**: `feat/sns-social`을 `main`에 병합·푸시해 Vercel이 최신 코드를 재배포하도록 함.
- **원인 2 — 앱인토스 빌드의 origin 불일치**: `apps-in-toss.config.ts`의 `webBundleDir: 'dist'`로 인해 앱인토스 빌드는 토스가 `dist`를 자체 호스팅함(Vercel과 다른 origin). 그런데 `src/platform/toss/login.ts`가 `fetch('/api/auth/toss/login')`처럼 **상대경로**로 호출해서, 배포가 되어 있어도 앱인토스 안에서는 이 경로가 실제 서버에 닿지 못함(로컬 `pnpm dev`·웹 배포는 같은 origin이라 이 문제가 드러나지 않았음).
  - **처리**: `src/lib/apiBase.ts`(`apiUrl()`) 신설, `VITE_API_BASE_URL` 환경변수로 절대 URL 구성. 웹 빌드는 빈 값(상대경로 유지), 앱인토스 빌드(`.env.toss`)는 `https://dawnpeople.vercel.app`으로 고정.
  - `api/auth/toss/login.ts`에 CORS 허용 추가(`access-control-allow-origin: *` + `OPTIONS` 핸들러) — authorizationCode가 실제 토스 로그인에서만 나오는 10분 만료·1회용 값이라 origin 제한이 보안상 의미가 크지 않아 와일드카드로 단순화.
- **확인 필요(TODO)**: 이번 수정 후 실제 토스 로그인 재테스트 필요(여전히 이 환경에서는 진짜 `TossAuth.login()` 호출을 검증할 수 없음 — 디바이스/샌드박스에서만 확인 가능). 배포 URL이 바뀌면(커스텀 도메인 연결 등) `.env.toss`의 `VITE_API_BASE_URL`도 같이 갱신해야 함.

## 2026-09-23 — 전면 광고 그룹 ID를 메뉴별로 분리(수다방/소모임)

- **배경**: 사용자가 앱인토스 콘솔에서 전면 광고그룹 2개를 발급받음 — 수다방 글쓰기 완료용(`ait.v2.live.9cfbaa73c3194289`), 소모임 개설 완료용(`ait.v2.live.c1ee80aa8e1846c4`). 기존 코드는 두 완료 이벤트가 광고그룹 ID 하나(`VITE_AD_INTERSTITIAL_ID`)를 공유하고 있었음.
- **처리**: `src/platform/toss/ads.ts`를 슬롯(`'feed' | 'bungae'`)별로 광고그룹 ID·로드 상태·5분 빈도 제한을 따로 관리하도록 변경. `showCompletionAd()`가 `showCompletionAd(slot)`으로 바뀌어 `ComposePage.tsx`는 `'feed'`, `BungaeCreatePage.tsx`는 `'bungae'`를 넘김. 환경변수 `VITE_AD_INTERSTITIAL_ID` → `VITE_AD_INTERSTITIAL_FEED_ID`/`VITE_AD_INTERSTITIAL_BUNGAE_ID`로 분리(`.env.example`, 로컬 `.env`에 실제 값 반영 완료).
- **⚠️ 사용자가 해야 할 일**: Vercel 프로젝트 환경변수에 있던 `VITE_AD_INTERSTITIAL_ID`를 지우고 `VITE_AD_INTERSTITIAL_FEED_ID`/`VITE_AD_INTERSTITIAL_BUNGAE_ID` 두 개로 새로 등록(값은 위 두 ID). 등록 후 재배포해야 반영됨.
- **`main` 병합**: 사용자가 "개발적인 판단은 알아서 하라"고 위임 → `feat/sns-social`을 `main`에 fast-forward 병합·푸시(`dbf70be..edd5b33`). 이전 세션에서 `main`에 못 올라가 있던 토스 로그인 버그 수정 3건(2026-09-23자)도 이번에 함께 반영됨 — Vercel 프로덕션이 재배포되면 로그인 수정분도 실서비스에 적용됨.
- **상세 화면 하단 배너 광고 구현**: `TossAds.attachBanner`(웹뷰 배너 SDK, `docs/apps-in-toss-notes.md` 82행) 사용. `src/platform/toss/ads.ts`에 `attachTossBanner`/`isTossBannerSupported` 추가(초기화는 `TossAds.initialize` 1회, 대기 중 언마운트되면 아무 것도 안 붙임). `src/components/BannerAd.tsx` 컴포넌트를 만들어 `PostDetailPage`(글 상세, 댓글 위)·`BungaeDetailPage`(소모임 상세, 참석 카드 아래)에 배치. 앱인토스 밖(웹)에서는 `isTossBannerSupported()`가 false라 렌더링 안 함. 환경변수 `VITE_AD_BANNER_ID` 미설정 시 공식 테스트 ID `ait-ad-test-banner-id`로 동작(전면 광고와 같은 정책 위반 방지 패턴).
  - **아직 실제 배너 광고그룹 ID를 못 받음** — 사용자가 전면 광고 2개처럼 배너용 광고그룹 ID를 콘솔에서 발급받으면 `VITE_AD_BANNER_ID`로 등록 필요(전면과 달리 메뉴 구분 없이 하나로 통일, 기존 결정과 동일).
- **성별/생년 복호화 실데이터 검증 완료**: 사용자가 Supabase SQL Editor에서 직접 조회해 스크린샷으로 확인. 실제 토스 로그인 계정 1건에서 `gender='female'`, `birth_year=1987`이 정상적으로 들어와 있음 — AES-256-GCM 복호화 파이프라인이 실데이터에서도 정상 동작 확인됨(2026-09-23 항목 4번 TODO 해소).
- **소모임 개설 한도·신고 자동 비노출을 실제로 강제(오래된 TODO 해소)**: `app_settings.BUNGAE_LIMITS`(`daily_create:2, concurrent:3, report_threshold:3`)는 0001_init.sql부터 있었지만 강제 로직이 없었음(2026-09-21/22 기록에 계속 TODO로 남아있던 항목, CLAUDE.md "하루 2건·동시 3건 한도, 신고 3건 누적 시 자동 비노출"). `0013_moderation_limits.sql` 원격 적용 완료.
  - **개설 한도**: `bungaes` BEFORE INSERT 트리거 `enforce_bungae_create_limits` — 호스트 기준 당일(Asia/Seoul) 개설 2건, 동시 진행중(open/full) 3건 넘으면 `raise exception`으로 차단. `BungaeCreatePage.tsx`는 기존 에러 처리(`err.message` 표시)가 그대로 이 메시지를 보여줌(코드 변경 불필요).
  - **신고 자동 비노출**: `reports` AFTER INSERT 트리거 `enforce_report_threshold` — 같은 대상에 서로 다른 신고자 3명 이상 누적되면 자동으로 posts/comments→`hidden`, bungaes→`cancelled`, bungae_comments→`hidden` 처리. `target_type='user'` 신고는 자동 정지 기능이 아직 없어(2026-09-22 기록) 자동 조치 없이 관리자 수동 처리만 남김.
  - 두 트리거 함수 모두 기존 카운터 트리거(0004)와 같은 패턴(`security definer` + `revoke execute ... from public, anon, authenticated`)으로 만들어 RPC로 직접 호출되지 않게 막음. `get_advisors(security)` 재확인 결과 새로 추가된 경고 없음(기존에 있던 항목들과 동일).
  - **아직 안 한 것**: 실제 계정으로 한도 초과 케이스를 수동으로 재현해보는 실사용 테스트는 안 함(로직은 기존 검증된 트리거 패턴을 그대로 따름). E2E 테스트에도 아직 반영 안 함.
- **글(수다방) 수정 기능 추가**: DB는 이미 준비돼 있었음(`edited_at` 컬럼, `posts_update_own_or_admin` RLS, `posts_guard_update` 트리거가 body/media/edited_at만 허용) — 실제 "수정하기" UI만 없던 상태였음("남은 일" 목록에 있던 항목). 댓글 수정에 쓰던 `EditBox`(`CommentSection.tsx`)를 export해서 `PostItem.tsx`에서 재사용, `MoreMenu`의 `onEdit`을 연결. 첨부 사진·동영상은 수정 범위 밖(댓글 수정과 동일한 텍스트만 수정 원칙, 삭제 후 재작성으로 대체 가능).
- **유출된 비밀번호 차단(HaveIBeenPwned) 활성화 시도 — 유료 플랜 필요라 보류**: Supabase 보안 어드바이저리에 뜬 "Leaked Password Protection Disabled" 항목을 Management API로 직접 켜보려 했으나 `402 Configuring leaked password protection via HaveIBeenPwned.org is available on Pro Plans and up`로 거부됨. 유료 플랜 업그레이드는 CLAUDE.md "먼저 물어볼 것"(유료 리소스) 항목이라 진행하지 않고 보류. 지금 Free 플랜에서는 비활성 상태 유지. **(2026-09-23 사용자 확인: 지금은 패스, 나중에 다시 검토)**

## 2026-09-23 — 최종 심사 준비: 소모임 개설 문구 유도, 공유 딥링크, 연동 해제 웹훅, 탭바 재점검

- **배경**: 사용자가 "다 끝나면 토스에 최종 심사 넣으려고"라며 남은 작업을 정리해달라고 요청. 배너 광고그룹 ID는 대기 중, "이름" 동의 스코프는 콘솔에서 필수로 고정돼 있어 뺄 수 없음(사용자 확인, 조치 없음), 유출 비밀번호 차단은 패스.
- **소모임 개설 화면에 건전한 활동 유도 문구 추가**: "새벽에 퇴근하고 혼자 심심한데 같이 운동·취미·식사해요" 식 제목/내용 placeholder + "이성 만남·소개팅 목적 제한될 수 있어요" 안내문(`BUNGAE_COPY.contentGuide`) 추가. CLAUDE.md 절대규칙 3(소개팅처럼 보이지 않게) 직접 대응.
- **공유(딥링크) 기능 구현**: 기존엔 웹 URL(`navigator.share`/클립보드 복사)만 있었음. 공식 문서(`Share.createLink`, `getTossShareLink`) 조사 결과 앱인토스 안에서 공유하려면 `intoss://<앱이름>/경로` 형태 딥링크가 필요하다는 걸 확인. `src/platform/toss/share.ts` 신설(`Share.createLink`+`Share.sendMessage`), `src/platform/index.ts`의 `shareAppPath()`가 앱인토스/웹을 자동 분기. 기존 `feed-api.ts`의 `sharePath`를 걷어내고 이걸로 통일, **소모임 상세에도 공유 버튼을 처음 추가**(전엔 수다방 글에만 있었음).
  - **한계**: `intoss://` 스킴은 **정식 출시 후에만** 실제로 동작함(공식 문서 확인). 출시 전에는 콘솔이 발급하는 테스트 스킴(`intoss-private://appsintoss?_deploymentId=...`)으로만 실기기 검증 가능 — 로그인 때와 마찬가지로 여기서 더는 검증 못 함. **사용자가 `ait deploy`로 테스트 빌드를 올린 뒤 실기기에서 공유→클릭→해당 글/소모임으로 이동하는지 확인 필요.**
- **토스 로그인 "연동 해제(UNLINK)" 웹훅 — 코드는 작성, DB 적용은 보류**: 조사 중 CLAUDE.md 절대규칙 4("토스 연결 해제 시 데이터 삭제")를 지금까지 아무도 구현하지 않았다는 걸 발견함(회원탈퇴용 `delete_my_account`만 있고, 토스앱에서 직접 연결을 끊는 경우를 받는 콜백이 없었음). 공식 문서 확인 결과 이 콜백은 **"이름·이메일·성별 외의 항목(우리는 생년월일을 받음)을 요청하면 등록이 필수"** — 선택이 아니라 필수 항목이었음.
  - **구현**: `api/auth/toss/unlink.ts`(GET/POST 모두 지원, `userKey`로 프로필 조회 → `admin_delete_account` RPC로 계정·글·소모임·대화 삭제 → 스토리지 파일 정리 순서. 삭제를 스토리지 정리보다 먼저 해서, 중간에 실패해도 "파일만 남고 계정은 지워짐" 쪽이 "계정은 있는데 파일만 지워짐"보다 안전하다고 판단). `admin_delete_account(uuid)`는 `delete_my_account()`와 로직은 같지만 `auth.uid()` 대신 특정 사용자 id를 받음(웹훅엔 로그인 세션이 없어서). 콜백 요청은 콘솔에 등록하는 Basic Auth로 검증(`TOSS_UNLINK_CALLBACK_USER`/`PASS`, 로컬 `.env`에 무작위 값 생성해 반영 완료).
  - **⚠️ DB 마이그레이션(`0014_admin_delete_account.sql`)은 작성만 하고 원격에는 적용 안 함** — 계정을 통째로 지울 수 있는 함수라 harness의 자동 승인 분류기가 "공유 리소스 수정"으로 막았고, 저도 사용자 확인 없이 진행하는 게 맞다고 판단(잘못되면 되돌릴 수 없는 개인정보 삭제라 리스크 급이 다름). **적용하려면 사용자가 마이그레이션 SQL을 검토 후 승인해야 함.**
  - **사용자가 추가로 해야 할 일**: 마이그레이션 승인 후, 앱인토스 콘솔에서 연결 끊기 콜백 URL(`https://dawnpeople.vercel.app/api/auth/toss/unlink`)과 Basic Auth(`.env`의 `TOSS_UNLINK_CALLBACK_USER`/`PASS` 값)를 등록하고, Vercel 환경변수에도 같은 두 값을 등록해야 함.
- **탭바 재점검**: 기존에 "TDS 공식 탭바로 반드시 교체해야 한다(미구현)"고 적혀 있던 게 실제로는 부정확했을 가능성을 발견함. 공식 문서 재조회 결과 탭바는 선택 사항이고, 쓴다면 "플로팅 형태"(토스 메인 탭과 안 겹치게)만 지키면 됨 — 지금 `TabBar.tsx`/`TabBar.css`가 이미 화면 하단에 떠 있는 캡슐형(고정폭 아님, 완전히 둥근 모서리, 그림자, 글래스)이라 이미 충족했을 가능성이 높음. 다만 정확한 세부 규정(치수·아이콘 스타일)까지는 문서로 확인 못 해서 **검수 신청 전 육안으로 공식 예시와 비교 확인을 권장**(상세는 `docs/review-checklist.md`).
- **다음**: `docs/review-checklist.md`를 오늘 기준으로 전면 갱신함 — 최종 심사 신청 전 남은 항목 정리는 그 문서의 "정리" 섹션 참고.

## 2026-09-23 — 헤더 여백·배경 수정, 알림(앱 안 알림 + 토스 푸시) 도입, 연동 해제 마이그레이션 적용

- **사용자 결정**: 채널톡 사전 문의 없이 바로 심사 제출해보기로 함(만남 서비스 분류 리스크는 심사 결과로 확인). 연동 해제 마이그레이션은 "적당한 때 알아서" → `0014_admin_delete_account` 원격 적용 완료. 탭바 육안 확인 OK.
- **상단 헤더**: ① 위쪽 빈 여백 — 앱인토스에서는 토스 내비게이션 바가 이미 상단 안전영역을 차지하는데 우리 헤더도 `env(safe-area-inset-top)`만큼 또 띄워서 이중 여백이 생기던 것. `--safe-top` 토큰을 두고 `data-platform="toss"`면 0으로(`main.tsx`에서 `data-platform` 지정). 헤더 높이도 56→52px, 위아래 패딩 8→4px. ② 배경이 뚝 끊기는 느낌 — 헤더가 연보라 반투명 단색 띠를 깔아 아래 배경 그라데이션·오브를 사각형으로 가리고 경계선이 생기던 것. 배경을 `::before`로 옮겨 거의 투명한 흐림 + 아래로 서서히 사라지는 마스크로 바꿈(내용은 마스크 영향 없음).
- **알림 3종**(사용자 요청 그대로): 내 수다글에 댓글 / 나에게 DM / 내 소모임에 참가.
  - **구조**: `notifications` 테이블 + DB 트리거(`comments`/`messages`/`bungae_participants` INSERT)가 알림 행을 만듦(`0015_notifications.sql`). 자기 자신·차단 관계·끈 종류는 만들지 않음. 클라이언트는 알림을 직접 만들 수 없고(RLS·RPC 실행권 회수), 읽음 표시만 바꿀 수 있음(guard 트리거).
  - **푸시**: 행동 직후 클라이언트가 `POST /api/push/dispatch`(Supabase 토큰) 호출 → 서버가 그 사용자가 만든 대기 알림을 DB에서 읽어 토스 스마트 발송(`send-message`, mTLS)으로 보냄. 문구는 DB 트리거가 만든 값만 써서 위조 불가. 같은 사람→같은 사람·같은 종류는 1분에 1번만(메시지 연타 시 알림 폭탄·과금 방지), 10분 넘은 대기 알림은 건너뜀. 템플릿 코드 환경변수가 없으면 앱 안 알림만 남기고 푸시는 건너뜀(`skipped`). 출시 전 테스트는 `TOSS_PUSH_TEST_DEPLOYMENT_ID`를 넣으면 `send-test-message`로 보냄.
  - **앱 안 알림**: 메시지 화면에 "메시지 | 알림" 탭. 푸시와 같은 내용(전체 미리보기는 40자)이 남고, 누르면 해당 글·대화방·소모임으로 이동. 상단 DM 버튼 배지에 안 읽은 알림 수 합산. 대화방에 들어가면 그 대화방 알림은 자동 읽음.
  - **푸시 누르면 이동**: 콘솔 템플릿의 이동 URL은 템플릿당 하나로 고정이라(변수로 글마다 다른 URL을 넣는 건 공식 문서로 확인 안 됨) `intoss://dawnpeople/notifications`로 두고 → 앱 안 알림 탭(`/notifications` → `/dm?tab=notifications`)에서 해당 글로 한 번 더 이동하는 방식으로 함.
  - **알림 끄기**: 종류별 스위치(`notification_settings`)를 내정보 → 설정과 메시지 화면(오른쪽 위 톱니바퀴) 두 곳에 같은 컴포넌트로. 끄면 앱 안 알림·푸시 둘 다 안 옴.
  - **문구 규칙(공식 문서)**: 기능성 푸시는 제목 7자 이내 명사형, 내용 25자 이내(변수는 2자로 계산) '~요.' 문장형. 사용자 초안("댓글이 달렸어요", "(닉네임)님으로부터 DM")은 이 규칙에 안 맞아 "새 댓글/새 메시지/새 참가자" + 내용에 닉네임·미리보기 변수 형태로 조정.
  - **2026-09-24 정정(콘솔 실제 화면 확인)**: 콘솔 변수 문법은 `#{nickname}`이 아니라 **`{{ nickname }}`**(`+ 변수`가 넣는 `{{ userName }}`과 같은 형태, 잘못 쓰면 "inbox template의 title, msg 중 올바르지 않은 중괄호" 오류). 본문이 변수만으로 구성되면 "푸시 작성 규칙" 위반이라 고정 문구가 필요함. 동의문 화면엔 변수별 예시문구 입력 필요. 푸시 4종(댓글·답글·소모임 댓글·메시지)은 `{{ preview }}` 하나만, 참가 알림만 `{{ nickname }}`·`{{ title }}` 사용. 제목은 7자 이내라 소모임 댓글 캠페인 제목은 "소모임 댓글".
  - **알림동의문 필수**: 댓글·메시지는 "꼭 받아야 하는 알림"이 아니라 사용자 동의가 있어야 발송 가능(캠페인=템플릿별 동의, 앱인토스 SDK `requestNotificationAgreement`로만 가능, 자체 구현 불가). 콘솔에 "새벽사람들 활동 알림 동의문" 1개를 만들어 5개 캠페인에 공통 연결. 앱 쪽 동의 요청 UI는 캠페인 승인 후 구현 예정(그 전엔 푸시 안 나가고 앱 안 알림만 동작).
  - **비용(⚠️)**: 공식 FAQ에 "스마트 발송: 건당 9.9원(vat 포함)", 워크스페이스당 10만 건 한도. 기능성 API 발송에도 과금되는지는 문서로 확정 못 함 → 과금 전제로 위 1분 제한을 둠. 템플릿을 등록·승인하기 전까지는 발송 자체가 안 되므로 비용 발생 없음.
  - **안 넣은 것(사용자 판단 + 조사)**: 팔로우·좋아요·소모임 참석자 대화 알림은 넣지 않음. 인스타그램·스레드는 좋아요·팔로우도 푸시하지만 묶어서(batch) 보냄 — 우리는 건당 과금 + 토스 기능성 메시지 기준(서비스 이용 유도·리텐션 성격이면 광고성으로 분류)에 걸릴 위험이 있어 제외가 맞다고 판단. 추가 후보로는 "참석한 소모임이 취소됐을 때(리더 취소·신고 비노출)" 참석자에게 보내는 알림이 가장 필요해 보임(빈도 낮음, 약속 관련 필수 정보라 기능성 기준에 맞음) — 사용자 확인 후 진행.
  - **개인정보 처리방침** 갱신(처리 목적·생성 정보·토스 위탁 항목에 알림 추가, `docs/legal-notion/02-privacy-policy.md` 동일 반영).
  - **검증**: 테스트 계정 2개로 실제 DB 통합 테스트 19개 항목 통과(각 알림 생성, 본인 행동 제외, 리더 자동 참석 제외, 끄기, 남의 설정 변경 불가, 직접 생성·RPC 호출 불가, 내용 수정 불가, 차단 시 제외, 디스패치 401/skipped, 알림 탭 이동) 후 계정 삭제. 실제 토스 푸시 수신은 템플릿 승인 후에만 확인 가능.
- **`ait deploy`는 harness가 "배포"로 분류해 막음** — 사용자가 직접 실행해야 함.

## 2026-09-24 — 푸시 캠페인을 5개 → 3개로 축소 ("새 댓글" 하나로 통합)

- **이유**: 캠페인마다 알림동의 화면이 따로 떠서, 댓글·답글·소모임 댓글(문구가 같음)을 하나로 묶어 사용자가 보는 동의 화면을 5개→3개로 줄임.
- **캠페인 3개**: `dawnpeople-comment`("새 댓글", `'{{ preview }}' 댓글이 달렸어요.` — 댓글·답글·소모임 댓글 공용), `dawnpeople-dm`("새 메시지"), `dawnpeople-bungae-join`("새 참가자"). 이동 URL은 모두 `intoss://dawnpeople/notifications`, 알림동의문은 "새벽사람들 활동 알림 동의문" 공통.
- **코드**: `api/push/dispatch.ts`의 `TEMPLATE_ENV`, `src/platform/toss/push-consent.ts`의 `PUSH_TEMPLATE_CODES` 두 곳이 같은 묶음이어야 함. Vercel env는 `TOSS_PUSH_TEMPLATE_COMMENT` / `_DM` / `_BUNGAE_JOIN` 3개(`_COMMENT_REPLY`·`_BUNGAE_COMMENT`는 더 이상 안 씀).

## 2026-09-24 — 전면 광고(글 작성·소모임 개설 완료) 제거

- **사용자 결정**: 글을 쓰고 나서 전면 광고가 뜨는 경험이 나빠서 수다방·소모임 전면 광고 2개를 모두 뺌.
- **코드**: `showCompletionAd`(`src/platform/index.ts`)와 `src/platform/toss/ads.ts`의 전면 광고 부분, `ComposePage`·`BungaeCreatePage` 호출, `VITE_AD_INTERSTITIAL_FEED_ID`/`_BUNGAE_ID` 타입·`.env.example` 항목 삭제. 글·소모임 상세 배너 광고는 그대로.
- **콘솔·Vercel**: 전면 광고그룹 2개는 콘솔에 남아 있어도 코드에서 안 부름(필요하면 콘솔에서 비활성화). Vercel의 `VITE_AD_INTERSTITIAL_*` env는 더 이상 안 쓰므로 지워도 됨.
- **CLAUDE.md 광고 항목** 갱신: 배너만 사용.

## 2026-09-24 — 푸시 동의를 가입 직후 자동으로 한 번 요청

- **문제**: 실기기 테스트에서 동의 화면이 자동으로 안 떠서, 사용자가 알림 설정의 "푸시 알림 받기"를 직접 누르기 전에는 푸시가 하나도 안 왔음(발송 API는 SUCCESS인데 미전달). 동의 후에는 댓글·DM·참가 푸시 모두 정상 수신 확인.
- **원인 정리**: 알림동의문은 5개(현재 3개) 캠페인에 공통 1개라 동의 화면은 한 번만 뜸. 자동 요청 코드가 없었음.
- **변경**: `useAutoPushConsent`(`src/features/notifications/`) — 앱인토스 안에서 프로필이 생긴(온보딩 완료) 계정에 대해 동의 화면을 계정당 한 번만 자동 요청(localStorage `dawnpeople:pushConsentAsked:<id>`). 거절해도 다시 자동으로 묻지 않고, 설정의 "푸시 알림 받기"로 언제든 재요청.
- **소모임 답글 알림**: 이미 구현돼 있음(`0017`의 `notify_on_bungae_comment` — 리더에게 댓글·답글, 내 소모임 댓글에 답글이 달리면 그 댓글 작성자(참석 중)에게. 푸시 문구는 댓글과 같은 "새 댓글" 캠페인). 코드 변경 없음.

## 2026-09-24 — 검수 반려 1: 내비게이션 바 뒤로가기와 미니앱 헤더 뒤로가기 중복

- **반려 사유**: "내비게이션 바의 뒤로가기 버튼과 미니앱 자체 헤더 및 뒤로가기 버튼이 함께 노출돼요. 중복된 버튼을 제거해 주세요."
- **원인**: 앱인토스 안에서는 토스 내비게이션 바가 뒤로가기를 이미 그리는데, 우리 `AppBar`(`src/components/ui.tsx`)가 `back` prop이 있으면 원형 뒤로가기 버튼을 또 그렸음.
- **조치**: 앱인토스 빌드(`VITE_PLATFORM=toss`)에서는 `AppBar`가 뒤로가기 버튼을 그리지 않고(제목·우측 버튼은 유지, 좌우 균형용 spacer만), 뒤로가기만 있던 헤더는 통째로 렌더하지 않음. 웹 빌드는 기존 그대로. 판별은 `src/platform/toss/navbar.ts` → `hasSystemBackButton()`(어댑터 경유).
- **그대로 둔 것**: 글쓰기 화면의 텍스트 "취소" 버튼(뒤로가기 화살표가 아니라 작성 취소 동작).

## 2026-09-24 — 검수 반려 2: 서비스 설명 없이 즉시 토스 로그인 유도 → 인트로 화면 추가

- **반려 사유**: "서비스 설명 없이 즉시 토스 로그인을 유도하고 있어 인트로 페이지 추가가 필요해요. 서비스를 소개하는 인트로 페이지가 먼저 노출된 후 토스 로그인이 진행될 수 있도록 수정해 주세요."
- **원인**: 앱인토스 빌드에서 로그인 진입(`/me`, 게스트 배너·"로그인하고 참여하기")이 곧바로 "토스 계정으로 시작해요 / 토스로 로그인" 화면이라 서비스 소개가 없었음.
- **조치**: `TossAuthPage`를 2단계로 — ① 인트로(서비스 소개 문구 + 수다방·소모임·닉네임 익명 활동 3가지 + "시작하기" / "먼저 둘러볼게요"(→ `/feed`)) → ② 토스 로그인(개인정보 최소 수집 안내 문구 + "토스로 로그인" 버튼). 문구는 `INTRO_COPY`·`AUTH_COPY.tossLoginNotice`(`src/config/copy.ts`). 웹(이메일 로그인) 빌드는 그대로.
- **참고**: 첫 화면(수다방 피드)은 원래 비회원 열람이라 로그인 없이 노출됨. 로그인 화면에 들어가기 전 항상 소개 단계를 거침.
