# 앱인토스(.ait) 빌드 전용. 비밀값은 없어요(그건 .env에서 읽음) — vite build --mode toss일 때만 .env 위에 덮어써요.
VITE_PLATFORM=toss
# 앱인토스 빌드는 dist를 토스가 자체 호스팅해 Vercel과 origin이 달라져서, /api 호출은 절대 URL이 필요해요.
VITE_API_BASE_URL=https://dawnpeople.vercel.app
